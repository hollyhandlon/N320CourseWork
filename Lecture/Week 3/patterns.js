var Library = {
    bookReturned: function(book) {
        //inform librarians of return
        //update book status
        //reshelve book
    },

    bookCheckout: function() {
        //calculate return date
        //update book status
    }
}